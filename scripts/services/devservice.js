'use strict';

angular.module('gepettoApp')
  .service('DevService', function DevService($resource) {
    // AngularJS will instantiate a singleton by calling "new" on this function
	
	return $resource('');
  });
